import { getPokemons } from '@/helpers/getPokemonsOptions.js'

describe('getPokemonOptions helpers', () => {
    test('debe regresar un arreglo de numeros', () => {

        const pokemons = getPokemons

        console.log(pokemons)


    })

})